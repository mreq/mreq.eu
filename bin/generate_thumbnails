#!/bin/bash
path=source/articles/images

mkdir -p "$path/thumbs"

for f in $path/*; do
  [ -f "$f" ] &&
  echo "Thumbnailing $f." &&
  convert -resize 930x600\> "$f" "$path/thumbs/$( basename "$f" )"
done
